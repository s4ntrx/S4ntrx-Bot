Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Flask-WTF==1.2.1
WTForms==3.1.2
email_validator==2.2.0
python-dotenv==1.0.1
Werkzeug==3.0.3
requests==2.32.3

# Only needed if you connect Postgres/Supabase (DATABASE_URL set in .env).
# Uncomment the line below for that — SQLite (the default) needs nothing here,
# and this package requires a C build toolchain on some Windows setups.
# psycopg2-binary==2.9.9
